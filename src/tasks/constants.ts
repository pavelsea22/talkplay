/**
 * Shared constants used across task activity components.
 * Centralised here so behaviour is consistent and easy to tune.
 */

/**
 * Number of failed attempts allowed before a task is marked as failed
 * and the lesson advances automatically.
 * Applies to both DrillWord and MinPairDiscrimination activities.
 */
export const MAX_RETRIES = 3;
